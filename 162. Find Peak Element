class Solution(object):
    def findPeakElement(self, nums):
        idx=0
        maxi=max(nums)
        for i in range(0,len(nums)):
            if maxi == nums[i]:
                idx=i
        return idx
        
